{
  "name": "metlake-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "node --experimental-strip-types --test 'src/**/*.test.ts'"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^5.0.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@duckdb/duckdb-wasm": "^1.29.0"
  }
}
